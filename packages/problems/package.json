{
  "name": "@codexa/problems",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit false --emitDeclarationOnly --outDir dist",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@codexa/types": "workspace:*"
  },
  "devDependencies": {
    "@codexa/config": "workspace:*"
  }
}
